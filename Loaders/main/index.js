const genreLoaders = require('../genreLoader');
const showLoaders = require('../showLoader');
const seasonLoaders = require('../seasonLoader');
const scategoryLoaders = require('../ScategoryLoader');
const tvcategoryLoaders = require('../tvCategoryLoader');
const movieLoaders = require('../movieLoader');
const sportLoaders = require('../sportsLoader');
const tvLoaders = require('../tvLoader');

module.exports = {
    genreLoaders,
    showLoaders,
    seasonLoaders,
    scategoryLoaders,
    tvcategoryLoaders,
    movieLoaders,
    sportLoaders,
    tvLoaders
}